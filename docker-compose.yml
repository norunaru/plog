services:
  spring:
    container_name: spring
    image: spring-app
    environment:
      - TZ=Asia/Seoul
    privileged: true
    networks:
      - app-network
networks:
  app-network:
    external: true
